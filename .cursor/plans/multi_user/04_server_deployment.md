# Шаг 4. Серверная установка и запуск (Windows и Linux)

Ссылка на основной план: [multi-user_server_scaling_2986b1e2.plan.md](../multi-user_server_scaling_2986b1e2.plan.md)

**Оценка:** 1–2 дня.

---

## Правило реализации

Перед началом реализации каждого пункта необходимо **согласовать** перечень подзадач с ответственным. После выполнения пункта — **отметить результат** в блоке «Итоги реализации» и поставить галочку в чекбоксе.

---

## Пункты плана

### 4.1. Конфигурация Streamlit для прослушивания по сети

- [ ] **Выполнено**

**Задача:** Настроить в [Planner-2.0/.streamlit/config.toml](Planner-2.0/.streamlit/config.toml) или через переменные окружения `server.address = "0.0.0.0"` (или аналог) для прослушивания всех интерфейсов. При необходимости — server.port, baseUrlPath, CORS.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 4.2. Документация: установка на сервер Windows

- [ ] **Выполнено**

**Задача:** Расширить [installer/README.md](installer/README.md) разделом «Установка на сервер Windows»: выбор каталога с правами на запись БД/логов, автозапуск (NSSM/служба или Планировщик заданий), рекомендация по обратному прокси (IIS, nginx, Caddy) для HTTPS и при необходимости базовой защиты.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 4.3. Документация и скрипты: развёртывание на Linux

- [ ] **Выполнено**

**Задача:** Добавить документ (например docs/deploy_linux.md или раздел в корневом README): установка Python/venv, копирование приложения, переменные окружения (DB, auth), запуск через systemd (unit-файл) с `streamlit run Planner.py`, пример nginx/Caddy перед Streamlit для HTTPS и при необходимости аутентификации на уровне прокси.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 4.4. (Опционально) Режим «серверная установка» в установщике Windows

- [ ] **Выполнено**

**Задача:** При необходимости добавить в установщик режим «серверная установка» (другой каталог по умолчанию или отдельный пункт в мастере), не меняя текущий сценарий «персональная установка».

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 4.5. Пример .env.example

- [ ] **Выполнено**

**Задача:** Добавить файл `.env.example` с перечислением переменных: PLANNER_DB_TYPE, DATABASE_URL, PLANNER_DB_PATH, путь к логам и др., с кратким комментарием по каждой.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

## Критерии приёмки шага

- Приложение можно запустить на сервере (Windows или Linux) с доступом по сети.
- Документация описывает автозапуск и при необходимости настройку обратного прокси для HTTPS.
