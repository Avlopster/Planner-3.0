# Шаг 3. Авторизация

Ссылка на основной план: [multi-user_server_scaling_2986b1e2.plan.md](../multi-user_server_scaling_2986b1e2.plan.md)

**Оценка:** 3–5 дней.

**Зависимости:** шаг 1 (аудит) — для подстановки user_id в log_action.

---

## Правило реализации

Перед началом реализации каждого пункта необходимо **согласовать** перечень подзадач с ответственным. После выполнения пункта — **отметить результат** в блоке «Итоги реализации» и поставить галочку в чекбоксе.

---

## Пункты плана

### 3.1. Таблица users и миграция

- [ ] **Выполнено**

**Задача:** Добавить таблицу `users` (id, username UNIQUE, password_hash, display_name, created_at, is_active и т.д.) в той же БД. Реализовать миграцию в [Planner-2.0/database.py](Planner-2.0/database.py).

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.2. Хранение и проверка паролей

- [ ] **Выполнено**

**Задача:** Использовать хеширование паролей (bcrypt или passlib). Реализовать функции проверки пароля при входе и (при необходимости) смены пароля. Добавить зависимость в requirements.txt.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.3. Экран входа и сессия

- [ ] **Выполнено**

**Задача:** Реализовать экран входа (форма логин/пароль). После успешной проверки сохранять в `st.session_state` user_id и username. При каждом переходе по разделам проверять наличие вошедшего пользователя.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.4. Защита маршрутов в Planner.py

- [ ] **Выполнено**

**Задача:** В [Planner-2.0/Planner.py](Planner-2.0/Planner.py) перед загрузкой основного контента (меню, дашборд, страницы) проверять наличие вошедшего пользователя. Если нет — показывать только форму входа и не рендерить дашборд и остальные разделы.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.5. Интеграция с аудитом

- [ ] **Выполнено**

**Задача:** При наличии `st.session_state["user_id"]` (или аналога) передавать его в `log_action(...)` (шаг 1). Для страницы входа оставить "anonymous" или не логировать вход до успешной аутентификации по политике.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.6. Выход и кнопка «Выйти»

- [ ] **Выполнено**

**Задача:** Добавить кнопку «Выйти» (например в сайдбаре). По нажатию — очистка сессии и отображение экрана входа.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

### 3.7. Управление пользователями (создание первого/новых)

- [ ] **Выполнено**

**Задача:** Определить способ создания пользователей: скрипт для первого пользователя, отдельная админ-страница или форма регистрации с одобрением. Реализовать выбранный вариант.

**Процесс реализации:**  
_(заполнить при реализации)_

**Итоги реализации:**  
_(заполнить после выполнения)_

---

## Критерии приёмки шага

- Без успешного входа доступ к дашборду и разделам приложения недоступен.
- В аудите фигурирует реальный user_id после входа.
- Пароли хранятся только в виде хеша. Бизнес-логика планирования не меняется.
